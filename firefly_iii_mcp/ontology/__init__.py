"""Instance-neutral Firefly III ontology owned by this distribution.

Data-only subpackage: it carries ``firefly.ttl`` (the ``owl:Ontology``
``http://knuckles.team/kg/firefly`` module) which the agent-utilities hub federates in via
the ``agent_utilities.ontology_providers`` entry point. It contains no record,
instance extension, generated signature, business logic, or runtime import.
"""
